* node wc.js wc.js
    * write high level test for wc
    * write count function
* Need to write right aligner
    * have to move util functions to util
* Node wc.js -l wc.js  
    * need to make count accept -l option      
* Node wc.js -c wc.js  
    * need to make count accept -c option
    * need to write parse.js
* Need to make wc work with combined options eg -lw
* Need to upgrade parse function to accept options in multiple arguments eg:"-l -w -c"